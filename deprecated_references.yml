# This file contains a list of dependencies that are not part of the long term plan for ..
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations .
---
packages/lawyer_profiles:
  "::Api::V1::Repositories::LawyerProfileRepository":
    violations:
    - dependency
    files:
    - db/seeds.rb
packages/organizations:
  "::Api::V1::Repositories::OrganizationRepository":
    violations:
    - dependency
    files:
    - db/seeds.rb
packages/users:
  "::Account":
    violations:
    - privacy
    files:
    - db/seeds.rb
