# This file contains a list of dependencies that are not part of the long term plan for packages/payouts.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/payouts
---
packages/organizations:
  "::Api::V1::OrganizationRecord":
    violations:
    - privacy
    files:
    - packages/payouts/app/models/api/v1/payout_record.rb
