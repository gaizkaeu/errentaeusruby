# This file contains a list of dependencies that are not part of the long term plan for packages/estimations.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/estimations
---
packages/tax_incomes:
  "::Api::V1::TaxIncomeRecord":
    violations:
    - dependency
    - privacy
    files:
    - packages/estimations/app/models/api/v1/estimation_record.rb
