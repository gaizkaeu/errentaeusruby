# This file contains a list of dependencies that are not part of the long term plan for packages/users/api.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/users/api
---
packages/users/domain:
  "::Account":
    violations:
    - privacy
    files:
    - packages/users/api/app/public/api/v1/serializers/account_history_serializer.rb
