# This file contains a list of dependencies that are not part of the long term plan for packages/users/domain.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/users/domain
---
packages/lawyer_profiles/domain:
  "::Api::V1::LawyerProfile":
    violations:
    - dependency
    - privacy
    files:
    - packages/users/domain/app/models/api/v1/user.rb
packages/organizations/domain:
  "::Api::V1::OrganizationMembership":
    violations:
    - dependency
    - privacy
    files:
    - packages/users/domain/app/models/api/v1/user.rb
