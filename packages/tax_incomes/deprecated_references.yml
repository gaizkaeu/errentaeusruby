# This file contains a list of dependencies that are not part of the long term plan for packages/tax_incomes.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/tax_incomes
---
packages/appointments:
  "::Api::V1::AppointmentRecord":
    violations:
    - dependency
    - privacy
    files:
    - packages/tax_incomes/app/public/api/v1/tax_income.rb
packages/users:
  "::Api::V1::UserRecord":
    violations:
    - privacy
    files:
    - packages/tax_incomes/app/public/api/v1/tax_income.rb
