# This file contains a list of dependencies that are not part of the long term plan for packages/appointments.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/appointments
---
packages/users:
  "::Api::V1::UserRecord":
    violations:
    - privacy
    files:
    - packages/appointments/app/models/api/v1/appointment_record.rb
