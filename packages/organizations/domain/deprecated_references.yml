# This file contains a list of dependencies that are not part of the long term plan for packages/organizations/domain.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/organizations/domain
---
packages/users/domain:
  "::Api::V1::User":
    violations:
    - dependency
    - privacy
    files:
    - packages/organizations/domain/app/models/api/v1/organization.rb
    - packages/organizations/domain/app/models/api/v1/organization_membership.rb
    - packages/organizations/domain/app/models/api/v1/review.rb
