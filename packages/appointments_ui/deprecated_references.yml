# This file contains a list of dependencies that are not part of the long term plan for packages/appointments_ui.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/appointments_ui
---
packages/appointments:
  "::Api::V1::Appointment":
    violations:
    - privacy
    files:
    - packages/appointments_ui/app/controllers/api/v1/appointments_controller.rb
    - packages/appointments_ui/spec/requests/api/v1/appointments_spec.rb
